# Copy to /opt/db.env (or .db.env). Only these keys are read for NE; JWT/other vars are ignored.
POSTGRES_SERVER=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_USER=sep
POSTGRES_DB=postgres
# Use single quotes if password contains $ ` " etc.
POSTGRES_PASSWORD='change_me'
